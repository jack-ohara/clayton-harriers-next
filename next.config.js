/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [
            'wp.claytonlemoors.org.uk'
        ]
    }
}

module.exports = nextConfig